// Problem: Create a JavaScript program that stores information about a person using variables.
const name = "Miyamoto Mushashi",
  city = "Shinjuku Prefecture",
  country = "Japan";

let age = 56;

age = 60;

console.log(`${name} lives in ${city}, ${country}. He is ${age} old. `);
